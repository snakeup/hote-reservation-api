# Datasource is overridden at runtime via @DynamicPropertySource in each integration test.
spring.datasource.url=jdbc:postgresql://localhost:5432/hoteldb
spring.datasource.username=hotel
spring.datasource.password=hotel

spring.jpa.show-sql=true
spring.flyway.enabled=true
